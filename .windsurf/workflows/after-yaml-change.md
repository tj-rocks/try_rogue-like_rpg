---
description: YAMLマスターデータ変更後にフォーマットチェックを実行する
---

## YAML変更後のバリデーション実行

以下を変更した場合に実行:
- `components/data/master/` 配下の `.yml` / `.yaml` ファイル

### 手順

1. YAMLバリデーターを実行する
```bash
cd /Users/tj/Desktop/2DGame && ./venv/bin/python tools/validate_yaml.py
```
